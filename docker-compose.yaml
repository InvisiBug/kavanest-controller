version: "3.1"

services:
  kavanest-skippy:
    container_name: kavanest-skippy
    image: kavanest-skippy
    build:
      context: .
      dockerfile: Dockerfile
    networks:
      - proxynet
    environment:
      - API=http://kavanest-api:4000

networks:
  proxynet:
    name: kavanest-devnet
